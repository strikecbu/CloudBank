apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudbank-common.fullname" . }}
  labels:
    {{- include "cloudbank-common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cloudbank-common.selectorLabels" . | nindent 4 }}
