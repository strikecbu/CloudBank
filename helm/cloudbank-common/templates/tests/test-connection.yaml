apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cloudbank-common.fullname" . }}-test-connection"
  labels:
    {{- include "cloudbank-common.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cloudbank-common.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
